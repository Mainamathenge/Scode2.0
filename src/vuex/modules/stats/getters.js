export default {
  stats(state) {
    return state.stats;
  },
  userGraph(state) {
    return state.userGraphs;
  },
  latestBusinesses(state) {
    return state.latestBusinesses;
  },
  latestExplorers(state) {
    return state.latestExplorers;
  },
  latestActivities(state) {
    return state.latestActivities;
  },
};
